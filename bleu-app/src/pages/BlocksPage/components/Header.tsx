import React from 'react';
import Box from '@mui/material/Box';

const root = {
  bgcolor: 'red',
  minHeight: '100px',
};

export default function Header() {
  return (
    <Box sx={root}></Box>
  );
}